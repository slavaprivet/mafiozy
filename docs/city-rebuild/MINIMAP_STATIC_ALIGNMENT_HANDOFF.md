# Миникарта: привязка значков к дорогам — 10 сентября 2026

Пользователь заметил съезжающие значки зданий при ходьбе и уточнил, что речь о значках относительно дорог, а не о движении всей карты под стрелкой.

В `exploration_minimap.mjs` статичные значки перенесены в общий с дорогами и контурами зданий кешируемый слой с запасом за краями. Убрано отдельное округление смещения этого слоя до физического пикселя. Значки, дома и дороги теперь имеют одну трансформацию. Динамические машины, NPC, поезд, персонаж и пользовательская метка сохраняются. При смене развёрнутого режима кеш обновляется даже при совпадении масштаба. Карта продолжает следовать за игроком с севером наверху; координаты наведения и установки/удаления меток не менялись.

Проверки: `test_minimap_static_alignment.mjs` исполняет действующую функцию рисования слоя на 30 кадрах с дробным перемещением, тремя DPR, повторным использованием/пересозданием кеша и сменой режима. Проверяет совпадение нарисованных координат дороги/значка с проекцией и координатами указателя. PASS. `test_exploration_minimap.mjs` и `test_map_object_index.mjs` также PASS.

LIVE: `/walk?standalone=1`, browser1/tab4 перезагружена; startupCompile rendered, error:null. На последовательных снимках при движении пользователя у автопарка Ист-Сайда значки и фон сохраняют взаимное положение (hero moving:true, x615.1603 z58.6648). Пользовательское управление для проверки не перехватывалось. Это проверка локальной карты; чёрный экран world рассматривает владелец startup compile отдельно.
