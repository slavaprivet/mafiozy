# Дороги и пешеходные подъезды — проверка фактического worker

13 сентября 2026. Проверен готовый `outputs/roads_logical_20260912/integration_candidate_snapshot.json`, SHA256 `756f4fb564e2ff81bfbb2dd00139a6add95c1319a3e4ca61a42adcfbabd89a40`. Снимок не изменился за проверки. Worker повторно не запускался; GPU-сцен не создавалось. Это заменяет промежуточные candidate-числа в предыдущих handoff, но не является приёмкой фактического движения NPC или FPS.

## Результат

- 39 парковок, 59 мест, все **46/46** обслуживаемых входов имеют пеший маршрут. Максимальный путь 164,473 м. Добавлены 22 общественных перехода и один служебный; сохранены 54 исходные зебры.
- **19 886** положений полной капсулы радиуса 0,38 м вдоль пеших маршрутов с шагом до 0,16 м: 0 ошибок. Проверены настоящие итоговые тела, допустимая поверхность, разрешённые crossing portals и метры маршрутов вместе с реальными 3D suffix башен.
- Все **77** переходов — 76 общественных и один service — имеют проверенные сухие подходы и связь с входом либо действующим парковочным маршрутом. Ошибок сохранённых подходов, размещения стоп-линий, фаз светофоров и пересечения полосы железной дороги нет.
- **6841** элемент разметки, **484** знака, **60** светофорных головок/**16** controllers; `unresolvedPlacement=0`. Аудит геометрии/направления/повторных знаков: 0 ошибок, 0 review.
- В prepared graph проверены **233** edge controls: 198 pedestrian decisions, 21 rail decision и 453 turn-crossing decisions. Занятый переход запрещает проезд; поддельный ID отвергается. Ранее поздние остановки у начала edge исправлены владельцем router: `shortStops=[]`.
- Использованы **109 фактических сериализованных** парковочных автомобильных маршрутов. На шести пересечениях с четырьмя зебрами настоящий external-path registry, wrapper и native `road-rules` дали правильные clear/occupied решения. Две отдельные регрессии stop projection проверили перенос остановки на реальный предыдущий участок assembled route. Аудит ~255 мс; это время CPU-проверки, не стоимость обновления всей игры.
- Service crossing `parking-crosswalk-21` на AL-022 не пересекается с этими парковочными кузовами; его канонические дорожные controls проверяются отдельно и сохранены.

## Открытая семантическая сверка с владельцем графа

`parking-crosswalk-23`, L-OL-H-011, центр `(303.485034,43.029790)`, нужен реальному пути old_town014. Его полоса `(303.227204,41.046479) → (303.742865,45.013101)` не пересекается ни с одним опубликованным prepared edge или фактическим driveway. Поэтому в graph-index у него пока нет control; нельзя считать это отсутствующим правилом без проверки наличия проезжающей полосы. Точные координаты отправлены владельцу graph для проверки, не удалялась ни зебра, ни demand.

Существующие rail crossings 0/1 также не пересекаются с опубликованными проезжающими edges; их реальные signal posts сохранены. Это тот же ранее переданный graph-owner review, без выдуманного проезда через закрытую геометрию.

## Артефакты и предел проверки

В `outputs/roads_logical_20260912/`:

- `final_worker_parking_pedestrian_audit.json`
- `final_worker_dressing_audit.json`
- `final_worker_crosswalk_access_audit.json`
- `final_worker_rail_controls_audit.json`
- `driveway_crossing_control_audit.json`

Все пять программ завершились с exit code 0 и status PASS; отдельные graph-review списки выше не скрыты. Финальная структура road renderer: **67 batches / 202 978 triangles**. До/после в read-only режиме — один и тот же план, это инвентаризация, не измерение ускорения. Поле `elapsedMs` pedestrian report в режиме `--existing` измеряет только выбор уже готового плана, его нельзя использовать как стоимость полного аудита или планирования.

Проверка общей сцены, frame time p50/p95, видимость в игре и source NPC follower принадлежат очереди координатора/Художника. Эти CPU-проверки не запускают поезд, машину или персонажа.
