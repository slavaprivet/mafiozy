# Чистый игровой интерфейс — 19 сентября 2026

По просьбе пользователя QA-панели скрыты по умолчанию даже при сохранённых `perfqa=1`, `npccombatqa=1`, `npctransportqa=1`, `mercenaryqa=1`.

- Новый `walk_debug_panels.mjs` подключён в `walk_preview.mjs`: маленькая кнопка «Проверка» слева сверху открывает/сворачивает существующие панели. Поля диагностики и игровые QA-объекты продолжают работать.
- Скрываются только перечисленные ID панелей и `[data-walk-hud="mercenary-showcase"]`. Карта, оружие, личное дело, приказы, подсказки, имена бойцов и таймеры действий не входят в список.
- Существующему контейнеру QA-конвоя в `world.html` добавлен ID `npc-transport-qa`; другой код мира не изменён этой задачей.
- Нет опроса DOM, наблюдателей или работы на каждом кадре. Одна таблица стилей и один переключатель. При раскрытии удаляется только правило скрытия: исходные display/состояние сворачивания панелей сохраняются.
- Локальное ограничение по hostname и явному параметру `*qa=1`. Переход страницы очищает кнопку и стили.

Проверка: Node smoke для закрытого умолчания, открытия/закрытия, сохранения игровых HUD-селекторов и dispose — PASS. Синтаксис `walk_preview.mjs` — PASS. Браузер не использован, LIVE и производительность общей сцены не проверены. Координатор загружает пакет и проверяет кнопку в единственной игровой вкладке.
