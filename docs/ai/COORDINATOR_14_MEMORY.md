# Память Координатора 14 — 2026-09-08

Текущая задача `01a08279-8066-7563-83fd-2d3fc4cbca41`, название «кординатор 14».
Пользователь попросил стать продолжением закреплённой задачи
«Продолжить задачу координатора 13» (`01a07dd8-48a5-7682-8da4-ab5d852fe551`).
Доступный диалог прочитан и сохранён в `COORDINATOR_14_INHERITED_TRANSCRIPT.md`.
Старые память/транскрипты 13 сохраняются как история; новые инструкции ниже
приоритетнее старых незавершённых планов.

## Последняя назначенная работа

Новое управление скоростью: обычный WASD 4.6 м/с, Shift 7.8, удерживаемый Alt
1.65 (обе стороны клавиатуры; Alt приоритетнее Shift). Исправлена причина
медленного Shift: результат stepHeroPosture раньше возвращал default speed,
теперь frame вычисляет posturePresentation с актуальными running/slowWalking.
Анимация получает те же параметры; crouch1.55/prone.8 сохранены. CPU реальные
keydown/keyup + расчёт frame, posture и hero GLB PASS. Вкладка /walk?animationqa=1
обновлена; LIVE DOM подтвердил Alt1.65 и возврат default4.6, errors[].


Новое уточнение после банков: один Space — обычный вертикальный/направленный
прыжок WASD, два Space — прежний Max Payne. Внедрено. После жалобы пользователя
на нераспознавание второго Space расширено окно до 500 мс, время берётся из
события клавиатуры, не из обработки кадра. Переход без нового подброса/сброса
траектории. LIVE две Space подтвердили mode=dive/count1/errors[], CPU реальные
keyboard bindings и физика PASS. Подробности `../city-rebuild/HERO_JUMP_HANDOFF.md`.
Последующие уточнения: убрать рывок вниз при переключении, взгляд по прицелу,
боковой dive ведущим плечом вниз, упасть на землю в выбранном направлении и
быстро встать, увеличить дистанцию. Выполнено: pose blend .24s, отдельные
travelYaw/aimYaw, наклон ~88° при посадке, .1s контакт + .35s подъём, dive speed
10.5 вместо6.5 (обычный3.5). Дополнительный floor solve после IK. 1348 actual
skin snapshots/все14grips PASS, живой D+Space→Space mode:dive/count1/errors[].
Эти более поздние изменения не относятся к предыдущему backup commit.
Последнее уточнение: полёт Max Payne чуть ниже, чтобы не быть лёгкой целью.
Целевой apex .80м вместо1.05м, плавный переход за.24s без рывка; дальность10.5м/с
и быстрый подъём сохранены, обычный прыжок1.05м. Pure keyboard/physics/surface PASS.
Ещё уточнение: сразу Ctrl/Z после прыжка. Реализован buffer во время полёта/
recovery, применяется при actual ground contact без ожидания .45s get-up.
Pose/camera blend .28s прямо в crouch/prone; новая hero_pose_transition.mjs.
Weapon IK выполняется после poseblend, затемground. ПовторCtrl/Z отменяет/
меняеточередь; blur/releaseControls отменяет. Последнее «плавность доработать»:
общий поворот pelvis предотвращает промежуточное вставание, origin оружия и
поворот груди смешиваются с landing; устранён скачок локтя свободной руки.
PASS 60 GLB landing cases, 1348 jump snapshots, 360 weapon clearance cases,
keyboard/physics/posture/surface и hero suites. LIVE после reload: A+Space,
Space,Z → prone height.62; D+Space,Space,Ctrl → crouch height1.25, y0/errors[].
Художник закончил исправление прикладов в weaponPose; его default pose сохранён.
Оптимизатор улучшил floor/ceiling AABB reject (его A/B 41.7→34.7 мс, ещё не 60fps)
и получил от пользователя поручение GitHub backup: ветка codex/walk-backup-20260908,
отдельный Git index без переключения shared main. Подтверждённый backup:
`codex/walk-backup-20260908-200719`, commit `2e6417ed52217253dd2941fdcead814de318fd8d`,
GitHub slavaprivet/mafiozy, 140 новых/изменённых файлов; ls-remote проверил
оптимизатор. Секреты и пользовательскую БД не включал. Более поздние текущие
правки не считать автоматически вошедшими в этот снимок.

Увеличить тесные помещения по назначению. Пользователь явно разрешил
увеличение здания снаружи вместе с помещением. Уточнил: банки разделить на
комнаты по world, хранилище — отдельная комната, пока только стены/размеры,
интерьер позже. Результат и проверки:
`../city-rebuild/BUILDING_SIZES_BANK_LAYOUT_HANDOFF.md`.
72 увеличенных здания и 3 новых банковских корпуса в shared `/walk` 18538.
Исходные ID/серверные механики не мигрировали и не удаляли.

## Расширение карты — 9 сентября 2026

Пользователь попросил леса, озёра, горы/уклоны/тропы, городской декор,
разные породы и размеры деревьев, железную дорогу и поезд, миникарту с метками.
Общая интеграция готова в `/walk`18538; подробности и пределы проверок:
`../city-rebuild/EXPLORATION_SHARED_INTEGRATION_HANDOFF.md`.
Пять лесных зон, два озера, девять дорог/троп, семь пород деревьев с25 гигантами,
фонтаны/памятники/лавочки/клумбы/часы; западная линия с двумя станциями
и настоящим движущимся трёхвагонным поездом. Нативный город4.1 сохранён.
Позднее пользователь попросил городской участок: линия теперь1543.8м,
из них313м внутри города, «Западный вокзал»,22м платформа,8м пандус к улице,
10 native-переездов. CPU-проверки PASS; итоговый LIVE фиксируется в handoff.

Последние уточнения пользователя: миникарта СПРАВА СНИЗУ, оружие СЛЕВА СНИЗУ.
Заголовок миникарты показывает район, расстояние — метры, ПКМ непосредственно
по метке удаляет её. Машины отображают цвет кузова, здания — значки назначения.
Zoom перерисовывает чёткий слой в разрешении экрана. Последний запрос отменяет
постоянные длинные подписи: на карте ТОЛЬКО ЗНАЧКИ, название при наведении.
Всё это проверено живым прогоном. Вкладку игры не закрывать.

Текущие параллельные задачи по прямому поручению пользователя: художник14
подключает исходные world NPC через отдельный gateway; оптимизатор — этажи,
лестницы/крыши; автомобильная задача — fleet12/капот/багажник/ремонт;
оружейная — Q owned/G drop/E pickup. Их незавершённые части не объявлять готовыми.
Наши rail/terrain/map hooks сохранять при их scoped walk изменениях.

## Другие действующие владельцы

- «Художник 14», `01a08226-d440-72b3-9226-894ea2213e46`: пользователь ставит
  задачи лично, дал прямое разрешение внедрять оружие/положение лёжа/эффекты.
  Делает hero_walk/hero_arsenal/artist14 modules и свои scoped walk hooks,
  ближний бой, вода/плавание/реакции. На момент этой записи его итоговое QA
  ещё выполняется. Не выдавать standalone NPC/server hooks за готовую миграцию.
  Актуальный shared handoff получен и прочитан:
  `../city-rebuild/ARTIST14_GAME_INTEGRATION_HANDOFF.md`.
  Recovery/hash: `D:/codex_release/artist14_GAME_v14_20260908/FILES.json`.
  Подтверждены его тестами: одноручный opener при удержании (160 кадров),
  исправленная поза лёжа/вытянутые ноги, реальные GLB и хваты всех 14 видов
  оружия. Финальный аудит геометрии также PASS по передаче художника:
  оба пола, 3 поворота, 720 кадров ползания; длины костей сохранены,
  выпрямление ног 98.735%, ладони около .07 м от пола, проникновений skin нет.
  Переданный SHA256 hero_walk:
  `2D8960B6E5583DBE147DCC5FCE30BE1A55EBF3B14BB03D4B905956773BA6BDE6`.
  Это уже shared v14, не старый checkpoint13. Не перетирать файлы.
- «Оптимизация — двери и стекло без потери…»,
  `01a081f5-5b23-73a1-bb1d-0e6091e45951`: закончил shared car/contact/blast/exit,
  освободил walk. Точный handoff `../city-rebuild/VEHICLE_CONTACT_BLAST_HANDOFF.md`.
  Вторая машина — физическая цель столкновения, ещё не доступна для посадки.
  Передавший агент сообщил LIVE и тесты; мы не повторяли его весь автопрогон.
- Старого координатора не возобновлять и не запускать дубли его автоматизаций.

## Постоянные правила и уточнения

- Последнее сообщение пользователя: здания «потом доработаем»; попросил
  объединить оптимизатора и Художника 14, затем подтвердил понимание, что
  они уже работают в общей `/walk`. Повторного переноса или отдельного
  архива не требуется. Дальше продолжаем именно эту общую сборку.

- Все старые механики, сервер, ID, собственность и интерьеры сохранять.
  `/walk` — физический preview, не завершённая игра world/game.
- Полиция и красный мост фиксированы, native M=4.1. Пользователь ждёт
  дальнейший рельеф: горы, пляжи, согласованные дороги и физика; текущая
  задача размеров это не реализовывала.
- Здание: одиночное E, обычный проход через дверь в той же сцене.
  Автомобиль: E удерживать .3 с, все четыре двери ведут к своим местам,
  водитель/руль слева; подсветку двери машины убрать, текстовая подсказка.
- Q коротко — взять/убрать последнее оружие; .2 с — арсенал.
  Мышь вращает камеру без кнопки, ПКМ — прицел/снайперская оптика.
- Обломки машины теперь постоянные до уборки/reset, случайный разлёт;
  это заменяет старые обещания исчезновения через пять секунд.
  Пули в стекло не отнимают HP кузова. Следы попаданий живут отдельно.
- Пользователь разрешил независимых субагентов. На этом этапе room_audit
  завершил банковские GLB и helper масштабирования участков. Ruflo/ToolSearch
  в доступных инструментах не найдены; используем файловую память.
- Рабочее дерево содержит много правок других задач и untracked модулей:
  не делать blanket git add/reset/checkout, не копировать старый walk целиком.
  Последняя узкая наша правка walk — дополнительный QA маршрут к хранилищу.


## Прыжки/перелезание по отдельному поручению — 9 сентября

Задача `01a087e2-a009-7873-a45f-8e1a4c2effc4`: пользователь уточнил именно walk.
Добавлены contextual Space vault/mantle/shore, высотные коллизии обычного прыжка,
поддержка верха препятствий, выход из плавания на берег и отдельная анимация.
Новые модули hero_traversal/world/pose/qa; hero_walk и artist14 не редактировались.
Узкие shared walk hooks и подсказки. Подробности и границы CPU/LIVE проверок:
`../city-rebuild/HERO_TRAVERSAL_HANDOFF.md`. Не перетирать эти hooks при новых
интеграциях лестниц, воды, героя и оружия. Обычный/двойной Space сохранён.


## Невидимые преграды и выход на скорости — 10 сентября

По скриншоту пользователя и новому сообщению исправлены: reserved police paving
как невидимая стена (только pedestrian runtime, исходные masks сохранены),
завышенные круговые коллизии52скамеек/7столов, зарывание tumble skin на склонах,
падение exit root прямо на дно воды. Новые native_pedestrian_surface и
vehicle_exit_surface, узкий optional floorHeight в hero.tumblePose и walk hooks.
Подробный handoff `../city-rebuild/WALK_COLLISION_EXIT_FIX_HANDOFF.md`.
Старые car_exit mechanics, художник/оружие/NPC и расположение2440объектов сохранены.
