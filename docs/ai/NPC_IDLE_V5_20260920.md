# NPC idle patch v5 — 20 September 2026

Applied in shared production. HTTP 18538 serves `physical-grid-v5`; the existing GPU game tab has NOT been reloaded or inspected by Artist18. This task's CUA inventory still has zero tabs. Do not open another GPU tab or disturb the coordinator.

## Changes

- Ordinary eligible Walk residents no longer receive a second random 1.2–3.8 second (archetype-multiplied) pause when their next route becomes ready. Existing deliberate rest/activity timers, protected roles and 2D timing remain intact. Actual source foot-branch regression: movement begins after 100 ms instead of 2,500 ms in the controlled case; existing rest still waits 3,100 ms.
- The route queue admits up to eight small searches within its existing 4 ms CPU budget. FIFO, one admission per owner, expiry and retained frontiers remain. Expensive work still consumes the budget before reaching the admission cap. This is a throughput improvement, not a claim that route waiting is solved.
- Wander search yields between neighbor edges and resumes the exact current node/direction. It previously finished an entire four-neighbor expansion after its deadline. Deterministic first-slice CPU: 5.74 → 4.10 ms, unchanged route/parent order and no duplicate candidates. The bound still permits one atomic edge of overrun.
- Native bench approach is 1.8 m ahead of the seat instead of 0.6 m; 2D keeps 0.6 m. Bench routes now use native A* with stable actor policy, body clearance and swept edges. Actual snapshot: 23/28 safe-start cases complete approach → sit → release, versus 0/28 before. Other cases refuse safely. A newly obstructing actual sedan causes one replan and the existing 60-second bench blacklist.
- Unavailable physical building access no longer holds a resident outside forever. After 3 seconds it abandons the visit at the saved real outside point; already-entered residents attempt a collision-checked exit. Blocked exits can use bounded native routing. Sealed rooms remain blocked until a route exists; no teleport or removal. Real shop purchase and six building visit cycles pass.

## Scope and measurements

Production edits: `world.html`; only the explanatory budget comment in `npc_native_directed_route_source.js`. No player vehicle bridge, police AI, crime reporting, car physics, population cap or collider removal. Test fixture loads the three new recovery helpers.

`test_npc_ordinary_loop18.mjs` runs 288 residents across actual geometry for 60 seconds at 10 Hz with actual ordinary source movement. This deliberately excludes visits/social/traffic scheduling and therefore does not reproduce the whole city. After the neighbor-yield patch, reverse-order comparison measured final-20-second movement 66.70% → 75.17%; pending 25.64% → 24.19%; automatic rest 7.66% → 0.31%. Ordinary-loop CPU p50 11.01 → 12.17 ms, p95 13.66 → 13.98 ms. More physical movement costs more; no full-scene FPS conclusion follows.

An earlier forward-order comparison before the neighbor-yield patch measured only 70.99% → 71.52% movement, with CPU p95 13.99 → 15.22 ms. Do not cherry-pick the stronger result or call this the completed living city. Route-wait p95 remains about 22–24 seconds under the 10 Hz synthetic stress scenario. The remaining queue latency requires further work and actual loaded-game diagnosis.

Key passing checks: ready-route timing; narrow-corner following; FIFO/CPU/admission fairness; preserved wander depth, goal spread and grid exit; unavailable door/blocked exit/death/stun; actual bench cycle and dynamic obstruction; actual print-shop purchase 55 → 52 and exit; post-car ordinary continuation. Six classic inline scripts compile. HTTP fetch confirms the production marker and changes, NOT the currently open page's execution.

Static cross-frame navigation cache was audited but NOT integrated: safe lifecycle invalidation is not available from the existing counters. Candidate files are isolated. Longer-distance visual seating still needs LIVE inspection; current renderer blends to the reserved seat over 0.5 seconds.

The user's latest comparison with working police is valid: `_movePoliceFootCop` attempts a collision-safe direct step before route search, whereas civilian wander waits for a complete outing. A read-only follow-up is evaluating whether safe route lookahead can reduce civilian waiting without destroying goals or reintroducing micro-walk loops. No such follow-up is applied in v5.
