# Художник21 — завершение группы очереди маршрутов, 23 сентября 2026

IMPLEMENTED / CPU TESTED. LIVE и производительность общей сцены не проверены.
Координатор20 владеет единственной игрой18538 и проводит reload/наблюдение.

## Доказанный дефект и узкий patch

В `_npcReserveRouteWork` (`world.html`) добавлена одна строка: обнуление
`_npcRouteWorkBatchCount`, если активная группа `_npcRouteWorkBatch` уже пуста.
Раньше группа, перенесённая из предыдущего кадра, после последнего допуска
оставалась закрыта до следующего кадра. Следующий житель получал отказ даже
при 0.25 мс использованного бюджета и одном из восьми допусков.

Не изменены 4 мс бюджета поиска, максимум восемь допусков, запрет второго допуска
одному владельцу в кадре, коллизии, `_npcFinishRouteWork`, водная маршрутизация,
наёмники, телефон и транспорт. Незавершённая старая группа по-прежнему имеет
приоритет. Канонического mirror этого scheduler в assets не найдено.

## Реальный исходный житель и границы доказательства

Экспорт checkpoint23, capturedAt `2026-09-22T21:39:41.165Z`: resident_205
на r41.5/c35.12539742494261, agenda=walk, phase=seek_shop, pending walk15.863с,
visited10/expanded9, последний grant6.903с назад. seek_shop здесь не актуальное
занятие. В water-before23 этот житель уже шёл; бесконечная блокировка не доказана.

`test_npc_resident205_cohort23.mjs` вызывает actual pickNpcWaypoint/native
navigation на его реальной позиции. В воспроизводимом допустимом состоянии
после завершения старой группы baseline не раскрывает ни одного узла, patch
продолжает поиск в этом же кадре (последний прогон12 узлов).
Это сконструированное состояние scheduler, не сохранённая внутренность LIVE:
экспорт содержит только scalar counts, фронтиры и точная история не экспортированы.

В отдельном прогоне77 pending walking позиций из того же экспорта actual geometry
даёт48 маршрутов до/после, включая205;0 непроходимых сегментов, максимум8 допусков.
Оставшиеся29 не завершились за60 симулированных секунд — этот patch не чинит их
геометрию/road exits. Время получения маршрута205 менялось между прогонами,
включая ухудшение; оно не доказывает устойчивого улучшения LIVE.

Последний CPU sample этого неполного сценария p50/p95:2.7495/5.7210→2.7783/5.9003мс.
Это измерение isolated planner, не full-world update или FPS; вариативность/JIT,
статический старый collision snapshot, нет других streamed cars/railway/GPU.
Статусы/позиции зафиксированы; поисковые фронтиры пересозданы, новые занятия отключены.

## Проверки

- `test_npc_route_cohort_drain23.mjs --require-fixed`: детерминированный actual
  scheduler,288 постоянных заявителей при7Hz/180сек. Средний допуск5.994→7.471,
  максимальный промежуток7.143→5.571с; максимум CPU4.25мс одинаковый
  (4мс + одна неделимая операция), не более8 допусков.
- `test_npc_route_batch19.mjs`: добавлена проверка каждого допуска — более старые
  необслуженные заявители остаются в текущей группе, никто не обходит их снаружи.
  При180 заявителях mixed max wait35→32кадра. Окончательный spread20–22 допуска
  допустим для группы из8, пересекающей границу кругов из180. Старую проверку
  spread<=1 заменили на<=2 вместе с более строгой пооперационной проверкой очереди
  и запретом ухудшения baseline max wait. Изменение ожидания теста явно учтено.
- `test_npc_route_cpu_budget18.mjs`, `test_npc_route_fairness.mjs`: PASS.
- `test_npc_activity_route_resume18.mjs`:13 PASS; activity agenda unit PASS.
- `test_empire_pending_slices19.mjs`:19/19 боссов завершили mixed и long-job
  сценарии; отмена/возобновление/одна slice per actor PASS. `test_npc_route_replay_diagnostics19.mjs`:PASS.
- `check_world.py`:7 inline scripts syntax PASS.
- Старый `test_npc_route_wait_lifecycle.mjs` падает одинаково с patch и без него:
  fixture считает `_npcRouteWorkCount=2` исчерпанием прежнего лимита. Тест оставлен
  без изменений. Это известный stale fixture, не скрытый PASS.

## Файлы пакета

- `world.html`: только строка сброса завершённой группы в `_npcReserveRouteWork`.
- `test_npc_route_cohort_drain23.mjs` и `outputs/npc_route_cohort_drain23.json`.
- `test_npc_resident205_cohort23.mjs` и `outputs/npc_resident205_cohort23.json`.
- `test_npc_route_batch19.mjs`: точная проверка старшинства и объяснённый spread.
- `assets/maps/city_rebuild_v1/test_fixtures/resident_walk_requests23.json`:
  scalar77 rows из реального экспорта, чтобы тест не зависел от `.git`.
- Этот handoff и `ARTIST21_MEMORY.md`.

Общий world содержит чужие WIP; не stage/commit всего diff как работу Художника21.
Commit/push ведёт координатор. ToolSearch/Ruflo не доступны в текущем tool inventory;
использована файловая память.

## Следующий LIVE

Обычный reload единственной игры после общего READY. После прогрева наблюдать205
и минимум двух обычных жителей через несколько циклов ходьбы/ожидания. Сохранить
route age/admittedAgo/workQuanta/queuePosition и реальные перемещения. На той же
камере/настройках снять frame p50/p95 и draw calls/triangles. Старый диагностический
тег `cohort19-v1` не менялся; source marker нового patch: `Reopen a drained cohort`.
Улучшение всего города и FPS не заявлять до фактического наблюдения.
