# Недоступная лавочка — Художник18, 19 сентября 2026

Source/CPU READY, LIVE не проверен.

В `_civilianPlanNext` окончательно недоступная лавочка сохранялась в `benchId`.
Каждые пять секунд житель выбирал её снова, даже если рядом доступна другая.
Actual selection fixture воспроизвёл 12 одинаковых отказов за 60 секунд.

Теперь окончательный отказ исключает эту лавочку на 60 секунд в рамках плана,
сбрасывает её выбор и разрешает следующую попытку через 200 мс. Ожидающий поиск
не является отказом: его цель и frontier сохраняются. Если все ближайшие лавочки
недоступны, план переходит к следующему посещению магазина. Истёкшие исключения
очищаются; временно заблокированное место впоследствии можно снова выбрать.
Занятая другим NPC лавочка по-прежнему учитывает резервирование.

PASS: test_npc_bench_retry_audit18.mjs (достижимая альтернатива со второй попытки,
сохранение pending, все места недоступны, восстановление после expiry),
test_civilian_purposeful_plan.mjs, test_npc_route_wait_lifecycle.mjs.
Первый тест использует настоящий выбор плана с контролируемым отказом route,
не выдаётся за проверку всей игровой геометрии или FPS.
