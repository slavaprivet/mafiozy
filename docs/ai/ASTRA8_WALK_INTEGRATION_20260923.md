# Восемь Астра → Walk, 23 сентября

## Последнее распределение — после ответа Проверщика2

Пользователь назначил Проверщика ЧАТОВ2 руководителем всей оптимизации и
устранения лагов с восемью Астра. Он подтвердил получение сводок всех8 и
сам переносит/проверяет optimization diff; root не дублирует эту работу.
Root обновляет текущий main, сообщает точный SHA и ведёт NPC/gameplay.
Готовые архивы1/2/6/7/8 основаны на527e9c0;3/4 audits,5regressionmatrix.
Старая проблема доступа ниже историческая, доставка теперь черезChecker2.
Новый NPClead Художник20, транспорт Автомобили — продолжение2; старые
остановлены пользователем. Одна root game18538 загружена, baseline сохранён
в COORDINATOR_20_MEMORY.md; LIVE улучшение FPS пока не доказано.

Поручение пользователя: получить готовые результаты восьми закреплённых
Астра, проверить и внедрить оптимизации и исправления (особенно посадку в
машину), продолжать NPC совместно с Художником19, принять живым прогоном.

## Получение исходных пакетов

Все восемь задач найдены; IDs в COORDINATOR_20_MEMORY.md. Попытки чтения
через read_thread вернули `ChatGPT conversation load timed out` для всех.
Прямая доставка результата запросом к Astra8 также не прошла: сообщение
не доставлено. Новых локальных архивов этих пакетов нет. Старые результаты
16–19 сентября не считать свежими пакетами пользователя.

Открыта одна вспомогательная вкладка ChatGPT Астра1. Веб-страница сначала
потребовала вход. Пользователь сообщил, что вход не грузится; затем сам
подтвердил, что загрузка пошла, и страница дошла до Google sign-in. Вход
сейчас завершает пользователь. Агент не меняет страницу во время входа.

Пакеты ещё не прочитаны, решения об их интеграции ещё не приняты.

## Независимое подтверждённое исправление

В Walk `updateCarInteraction()` перенос существующего
`sourceVehicleAccess.poll()` перед `sourceVehicleActive()` устраняет зависшее
pending после завершения выхода или прерванной посадки. Остальные правила
доступа/занятости/замков/source ownership не меняются.

Проверено actual source + actual adapter + actual host:

- offline exit, online exit ACK, interrupted entry — 3/3 PASS;
- world vehicle player access, car entry, male/female actual player pose — PASS.

Тест: `test_vehicle_entry_pending23_audit.mjs --require-fixed`.
Отдельный baseline/repro: `VEHICLE_ENTRY23_READONLY_AUDIT.md`.
Это исправление root, не приписывать его ещё не полученному пакету Астра.
LIVE цикл «сесть → выйти → снова сесть» ещё предстоит.

## Перед дальнейшими изменениями

Снимки восьми ключевых файлов и SHA256 сохранены в локальном ignored
`.git/ai-pipeline-local/baselines/astra23-before/`. Они включают исправление
pending и все прежние shared правки, но никаких новых оптимизаций Астра.
Не применять старые full-file replacements поверх shared каталога.

HEAD остаётся 527e9c0, много чужих незакоммиченных пакетов. Никаких broad
revert/stash/commit. Сопоставимые loaded-scene frame p50/p95, draw calls,
triangles и наблюдение NPC нужно снять до/после. CPU тесты не доказывают FPS.

Root берёт интеграцию/живой прогон одной игровой вкладки. Художник19,
Проверщик2 и транспорт уведомлены о границах; ответы пока не получены.
Игровые вкладки в root ещё не открыты: только вспомогательная ChatGPT.
