# Crew routes — 19 September 2026

User-reported failure: an engineer ordered to cut the cage from its opposite side oscillated against the wall. The same local-angle steering caused follow/rally crew to move side to side. Source movement now follows a stable collision-checked route instead of choosing a new avoidance angle each frame.

Scope: `mercenary_world.js`, `mercenary_core.mjs`, new `mercenary_route.mjs`, `test_mercenary_route.mjs`. No global NPC route ownership/hooks changed. Root owns authored object approach distances, vehicle side pin, visual pose reach and LIVE acceptance.

- Incremental local cardinal A* on a 0.65 m grid; maximum 2,400 nodes and 48 m route cost envelope. Checks every edge through the existing source body/path predicate, the rendered native predicate and nearby crew clearance. No position teleport or collision bypass.
- Applies to specialist approach, follow, rally and armed retreat. Order semantics, pending effect receipts and detonation safety stay authoritative. Waiting for a route stops movement rather than alternating local avoidance angles.
- Search advances at most 8 nodes / approximately 0.75 ms per job; all crew share a best-effort 1.5 ms search window per update with rotating roster priority. A collision query/one expansion cannot be preempted, so this is an admission/continuation budget, not a hard real-time guarantee. Direct-route validation and actual movement predicates also have their normal cost outside search slices.
- Waypoints are retained until the goal moves more than 0.35 m for specialist phases or 0.8 m for follow/rally, the order changes, or a physical step remains blocked for 0.5 s. Failed searches retry after 2 s. Every actual movement step still checks fresh collision/crew state. Active route progress prevents the 10 s no-progress timer from cancelling a legitimate detour; the total 40 s approach timeout remains.
- Core authored work-range minimum is now 0.05 m, accepting root's 0.08 m approach tolerance. The source footprint remains 0.18 source tiles = 0.738 m at scale 4.1. Root increased authored approach offsets accordingly; the previous synthetic 0.3 m body fixture did not establish real game clearance.

Validation: selected existing source/core/actions/lifecycle/focus plus route suite **100/100 passed** before one added CPU comparison; final dedicated route suite **6/6 passed**. Tests extract actual `_npcBodyPassable` and `_npcPathPassable` from `world.html`, with a swept cage collision fixture, preserving the actual 0.18-tile footprint. A cutter starts behind a 6×5 m cage, physically passes its side, reaches the front approach at -0.92 m with workRange 0.08, and cuts exactly once. Five-member follow and rally cases both walk around the cage and settle. Blocked search cancellation preserves position.

Comparable source CPU fixture, 700 updates, same cage/start/target: previous steering p50 **0.0365 ms**, p95 **0.0875 ms**, cut count 0; new routing p50 **0.0509 ms**, p95 **0.1392 ms**, cut count 1. This is a synthetic source CPU fixture, not loaded scene frame time or GPU performance. General scene performance and the user's live backside case remain coordinator-owned acceptance.

## Interruption, diagnostics and posture follow-up

- Source HP loss is checked before ordinary action progress/effect submission, including the frame crossing timer zero. Cutting, power, safe opening and bomb planting stop with `reason:'damaged'`; death remains `member_unavailable`. Effects and money are not granted. Already armed charges and submitted/pending receipts keep their existing authority and are not cancelled retroactively. No new damage path was introduced.
- A damaged worker holds its current point, preventing the medic's automatic scan from immediately reissuing the interrupted rescue. Explicit X can assign new work; V clears this hold. Regression covers interrupted medic, explicit successful revival, then V: both medic and revived bruiser physically resume following.
- `getMember(id).movement` is a read-only local-QA projection: `reason`, `search:{expanded,checks,slices}`, `pathLength`, `pathIndex`, `goal`, `routeGoal`, `lastAction:{kind,targetId,phase,reason,at}`. Reasons distinguish pending/budgeted search, no route, source/native/crew blocking, arrival and movement. It is absent outside QA. Terminal action reason survives after active action removal.
- Actual bounded search expansion refreshes the no-progress deadline while the search is active; the absolute 40-second approach limit still applies. Searches that stop progressing cannot claim indefinite activity. A reproduced V-after-revive diagonal search previously expanded 744 nodes/93 slices; an admissible cardinal-distance heuristic and goal-directed tie-break now reduce unnecessary exploration. A single collision-checked shortcut of up to six waypoints is attempted at most every 0.5 s; it never skips the swept collision predicate.
- Crew posture reads existing `_effectivePlayerStance()`. Own source snapshots publish `crouching`/`prone`; `getMember.posture` exposes the resolved stance. Crouch moves at 1.5 m/s, prone at 0.65 m/s, standing preserves 3 / 5.5 m/s. Work/awaiting and downed poses retain precedence. Other NPCs and the separate legacy combat movement loop were not rewritten; combat movement speed therefore remains outside this posture patch's acceptance.

Latest selected source/core/lifecycle/focus/route suites: **114/114 PASS**. Actual LIVE safe opening was still unconfirmed when handed off; root must inspect movement diagnostics rather than infer success from these tests. Updated same CPU fixture after heuristic/shortcut changes: old p50/p95 **0.0349/0.0854 ms**, new **0.0477/0.1202 ms**. These are CPU-only fixture timings, not scene FPS.

## Vehicle approach and 60 m focus follow-up

- User subsequently confirmed opening the safe and collecting its money. Root then observed the safecracker's post-opening route failing with one expanded node/four rejected edges. That pattern indicates the starting footprint cannot escape in any cardinal direction; an opened-door overlap remains a separate LIVE geometry investigation, not a failed safe unlock.
- Confirmed demo-car mismatch: actual `CAR.halfWidth` is 1.28 m, but the target descriptor formerly fell back to 1 m when the demo object lacked width metadata. Its approach at 1.84 m (plus 0.068 m stop margin) remained inside the physical clearance requirement 1.28 + 0.738 = 2.018 m. Root owns the target/profile fix. New source regression preserves the real body/path functions, proves the old anchor blocked, and proves the 1.28 + 0.84 m anchor supports planting, physical retreat at least 8 m from car centre, and exactly one explosion.
- Focus assignment is explicitly bounded at 60 m from the player and uses adapter `hasLineOfSight(fromFootWorld,toFootWorld,targetId)`; the existing source police line check is the compatibility fallback. Root wires the rendered LOS callback. Existing own/dead/friendly/authority exclusions remain.
- A focused NPC beyond the unchanged gang firing range now receives a source-owned collision-checked approach. Once in range, the ordinary gang loop owns combat. No firing distance, damage or cadence increase. A 55 m test observes zero shots before approach and ordinary source hit callbacks afterward; >60 m and blocked LOS reject assignment.
- Low-FPS timing remains a known limitation: crew displacement still uses `speed * min(dt,0.05)`, so at five updates/second 3 m/s walking can become 0.75 m/s while action timers use wall time. This task did not alter movement clocks without coordination with the NPC owner. Parent is evaluating the elapsed-time fix separately.
