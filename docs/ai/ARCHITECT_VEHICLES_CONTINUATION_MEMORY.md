# Продолжение архитектора — автомобили

Текущая задача: `01a087f0-fda6-7e13-8baa-1bbd1c5cc26e`.
Предшественник: «Архитектор — штабы и жилые дома»,
`01a06e4c-ecc1-7712-a720-86e4a9bef2fb`.
Пользователь 9 сентября попросил прочитать весь чат и продолжить здесь.

## Восстановление контекста

`read_thread` возвращает лишь старые завершённые этапы и два пустых прерванных
хода. Прочитан полный локальный JSONL предшественника (120 MB), включая
последние сообщения 9 сентября. Реплики сохранены в
`ARCHITECT_HQ_INHERITED_MESSAGES.json`, передачи между задачами — в
`ARCHITECT_HQ_INHERITED_DELEGATIONS.json`. Инструмент Ruflo/ToolSearch в этой
сессии отсутствует; используется файловая память.

Старые здания: civic v3, urban 3, rural 6 и landscape kit, compact glass tower;
последний реестр — outputs/architect_hq_latest_ready_asset_manifest_v1.json
в рабочей папке предшественника. Эти результаты сохраняются, однако они
не являются последней задачей пользователя.

## Действующая задача пользователя

9 сентября пользователь переключил архитектора на автомобильную физику:
вмятины, деформации, отрыв настоящих частей, повреждения мотора/ходовой,
ухудшение движения после аварии, все 12 авторских автомобилей плюс исходный
красный седан. Разрешены независимые субагенты. Целевая платформа — Steam.
Не выдавать ограниченную игровую физику за полную симуляцию BeamNG.

Уточнения: капот и багажник открываются/закрываются на E; под капотом двигатель,
радиатор и патрубки, дым при повреждении и ремонт при открытом капоте.
В багажнике нужен физически свободный объём для будущих предметов.
Проверить КАЖДУЮ машину: детали ошибочно висят отдельно от кузова, эталон —
первый красный седан; улучшить анимации крышек и доступ к мотору.
Название машины показывать в подсказке посадки.

Более поздняя передача от задачи магазинов отменяет реальные марки:
Kingswell (исходный красный), Brooklyn SX, Easton S, Brooklyn LX,
Bellhaven V8, Ravelli GT, Blackridge, Union Van, Ironvale, Metroline 90,
Patrol LX, Union Medic, Ironvale F. Сохранять текущие display labels/IDs/hashes.

## Состояние при приёме

Исходная папка:
`C:/Users/Слава/Documents/Codex/2026-09-05/mafiozy-architect-hq-replacement`.
Текущие черновики: work/vehicle_physics_20260909/HANDOFF_DRAFT.md и
VEHICLE_GEOMETRY_QA_HANDOFF.md. Staged modules/vehicle_fleet_body.mjs ещё
не был установлен в shared; staged factory также содержит старые названия и
позы, поэтому полная замена shared недопустима.

В shared уже подключены fleet, crash mechanics, hood/trunk, seats и prompts.
Начальный независимый прогон: 76 tests, 74 pass, 2 fail — доступ под капот
фургона перекрыт Gear_console; service geometry обнаруживает 63 дефекта
полостей/проёмов. Отдельный старый continuity baseline: 63 tests, 21 pass,
42 fail. Это входные результаты, не итог.

## Ведение работ

- fleet_geometry владеет только vehicle_fleet_models.mjs и vehicle_fleet_body.mjs.
- root владеет hood/trunk при необходимости, итоговой памятью и handoff.
- vehicle_regression — независимый read-only прогон.
- fleet_visual_proofs — только tools/vehicle_fleet_qa и outputs/vehicle_fleet_continuation;
  offline-рендеры реальной runtime-геометрии с RoundedBoxGeometry.
- Не перетирать walk, hero, weapon, NPC, world/server, rail/terrain/map hooks.
- /walk — общая проверочная сцена, не завершённая миграция world/server.
- Открытую пользовательскую вкладку не закрывать.

## Блокеры проверки при приёме

CUA дважды возвращает `Unable to load browser request-header policy` ещё при
инвентаризации вкладок. Обход UI другими технологиями не выполняется.
Offline-рендеры и CPU не называются LIVE-проверкой.
Независимый аудит также обнаружил отсутствующий npc_boss_art_direction.mjs
в import chain walk → npc_gallery_host/population; это область параллельного
художника, сюда не переносить чужой NPC-код.

## Завершённый этап — 10 сентября 2026

Установлено в общей /walk: восстановленная геометрия всех 13 машин, свободные
моторные/грузовые отсеки, исправленные посадки и руки на руле, реальные отрываемые
авторские бамперы. По дополнительной просьбе пользователя сильные аварии усилены:
фронт 16–22 м/с даёт примерно в 1,8 раза больше смятия, лёгкие касания сохранены.
Двигатель следует деформации кузова, ремонт не выпрямляет металл.

Итог: vehicle_all_final.log 150/150; после последних узких правок QA подхода
walk 5/5, после привязки двигателя ещё 21/21 соответствующих проверок.
Это пересекающиеся наборы, не складывать. Живой прогон CUA восстановлен;
NPC-модуль добавлен его владельцем. Все 13 моделей загрузились, проверены
26 циклов капот/багажник, front18 красной и авторской машины, ремонт двигателя.
Последний reload с новым креплением двигателя: оба удара проверены повторно,
console errors[]. Исходные пользовательские вкладки не закрывались.

Полная передача и границы этапа:
../city-rebuild/VEHICLE_FLEET_CONTINUATION_HANDOFF.md.
Предыдущий раздел блокеров описывает только состояние при приёме, блокеры сняты.

## Следующий завершённый этап — колёса, огонь, инерция, такси, цвета

Пользователь последовательно попросил исправить диски/вращение/арки, улучшить
огонь/взрывы, качать героя при ударе, разные штампы/литьё, добавить отсутствующее
такси и разные серые/чёрные/красные салоны. Всё подключено к shared renderer.
Теперь 14 моделей (добавлен отдельный city_taxi), разные физические диски,
14 интерьерных палитр, реальные ниши колёс, вращение шин красного исправлено.
GPU-пожар/дым/вспышка/искры заменили многогранники. Реакция головы/корпуса и
камеры подключена к контактам; руль/посадка/масштабы сохранены, red root -.03 м
устраняет исходное пересечение крыши. Полный vehicle набор:173/173 PASS.

Живой QA прошёл: такси/палитры, движущееся колесо (distance/r совпадает),
наклон водителя, пожар и настоящий взрыв с обломками, final console errors[].
Другая задача теперь направляет обычную /walk в world.html; для изолированного
QA использовать /walk?standalone=1&carqa=1. Маршрутизацию мы не меняли.
Подробнее: ../city-rebuild/VEHICLE_WHEELS_FIRE_IMPACT_TAXI_HANDOFF.md.

## Текущий этап — коллизии, повороты, дрифт

Пользователь показал невидимое препятствие возле пожарной и попросил доработать
управление. В shared подключены физические выпуклые контуры всех14 кузовов,
без прежнего запаса10–18см; collider отдельно от seat/water halfWidth/Length.
Новая модель шин/боковой скорости/руля/ручника и normal-based contact motion.
Сохранены crash/tyre effects, массы, импульсы, посадки, вода и чужие hooks.
Временный ответ неподвижной стены больше не останавливает машину за
удаляющимся другим автомобилем. Предел руля±.56 сохраняет зазоры арок.

Полный vehicle+car набор196/196 PASS; после последних изменений только QA
целевые6/6. LIVE: red проехал24.879м мимо fire с gap.12м без contacts/урона,
red плавный поворот прошёл без contacts. Финальный sport drift PASS: peak
18.186м/с, slip25.86°, боковая скорость3.218м/с, contacts0, сцепление вернулось,
машина остановилась. Первый прогон вышел за границы малой QA-площадки и задел
реальный дорожный знак; повтор сделан на проверенном участке70×70м с разгоном
до18м/с через обычный input. Physics для сокрытия этого контакта не менялась.
Final console errors[]. QA вкладка4 markedDeliverable, водитель в Ravelli GT
на стоянке; игровая вкладка3 сохранена. Этап завершён.
Передача: ../city-rebuild/VEHICLE_COLLISION_DRIVING_HANDOFF.md.
Текущие LIVE доказательства: outputs/vehicle_fleet_continuation/driving_live_qa.json.

## Правило производительности — последующее поручение координатора 15

Прочитан новый раздел shared AGENTS.md «Производительность — ответственность
каждого автора». Следующие собственные изменения передавать с сопоставимыми
замерами до/после: одинаковые камера/настройки/население/прогрев, frame p50/p95,
draw calls/triangles для графики и update cost для логики. Свои регрессии устранять
сразу, сохраняя механику, коллизии и визуал. Тяжёлый GPU-прогон согласовывать
с координатором; сейчас GPU занят им. Эта передача не требует заново запускать
завершённый автомобильный этап. Его LIVE подтверждает поведение, а не приёмку
производительности всей игры. Полного сопоставимого performance before/after нет.
