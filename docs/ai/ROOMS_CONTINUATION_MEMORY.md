# «Помещения» — продолжение 10 сентября 2026

Текущая задача `01a087fd-c50a-7311-bec6-6151109f010c`. Пользователь: «продолжи чат "помещения" он завис».
Источник `01a087ee-aa91-7ba0-b0cf-3048e48a4296` («Помещения»), idle/interrupted, worktree `51b3`.
Последние четыре хода, включая унаследованные публичные сообщения, сохранены в `ROOMS_INHERITED_DIALOGUE.json`.
Старую задачу не возобновляли, новые задачи/автоматизации не создавали.

Последние поручения: убрать голову, закрывающую обзор внутри; закрепить перила;
проверить наружные подъёмы на крыши; при входе «Название здания — открыть дверь».
Ранее 290 комнат/903 предмета/256 дверей уже интегрированы — не делать заново.
Прочитаны память Координатора 15, ART_DIRECTION_CANON, передачи помещений/этажей.
Ruflo/ToolSearch в каталоге недоступны; используется файловая память.

## Внесено в общий Desktop/Мафиози

- `indoor_camera.mjs`, `hero_visibility.mjs` из проверенных заготовок `.interiors-camera`.
- Узкие hooks в свежем `walk_preview.mjs`: restore до анимаций/геометрических запросов,
  solve после орбитальной камеры, маска конечностей только перед render, restore перед сменой героя.
  В тесноте горизонтальный зазор <2.8 м включает eye view, >3.4 м возвращает orbit.
  Руки/ноги/оружие сохранены; скелет, GLB и hero renderer не изменены.
- `building_prompt.mjs` подключён для наружных дверей; внутренние подписи сохранены.
- Агент rail_roof интегрировал только `interior_staircase.mjs`, `roof_ladder.mjs`, helper и тесты.
  Подробности: `docs/city-rebuild/STAIR_RAIL_CONNECTIONS_HANDOFF.md`.
- В существующем opt-in `?buildingqa=1` добавлены три кнопки размещения возле
  внутренней/наружной лестницы и на верхней точке наружной. Это диагностика;
  само лазание по-прежнему запускается штатным E. Обычный /walk этих кнопок не показывает.
- Backup именно нашего начала: `outputs/rooms-continuation/walk-before.mjs`.
  Не копировать его обратно целиком: параллельно другие владельцы редактируют общий walk.

## Проверено

CPU: indoor eye/hysteresis/posture/reset PASS; маска actual GLB PASS (552 треугольника
кистей,408 предплечий сохранены; head/neck отсутствуют, 50 циклов reuse/restore);
80 настоящих направлений indoor camera PASS; банковские семь комнат/75 footprints PASS;
клавиши/прыжки PASS; выход из машины/камера PASS; recoil view PASS.
Агент: 3600 стыков перил/96 конфигураций PASS, 6 крыш/8516 позиций PASS.

CUA browser работает, in-app браузер 1, tab 1, `http://127.0.0.1:18538/walk?buildingqa=1`.
LIVE: обычный E открывает oldtown и hospital; глазная камера включается, supported=true,
hidden=true, руки сохранены. Больница: чистый вид коридора с дверями и лестницей;
cameraY=feetY+1.64. Выход через штатное движение: orbit/hidden=false, полный герой
визуально восстановлен снаружи. Ошибок console при загрузке не было.
У oldtown QA прямой путь упирается в близкую мебель: лицо уже скрыто, но ракурс
на шкаф не означает художественную проверку всего помещения.
LIVE лестницы завершён: малый банк E вверх y0→y5 и E вниз y5→y0, указатель и опоры
осмотрены. В больнице видны закреплённые поручни. Первый прогон показал, что
порог1.8 оставлял крупную голову на площадке при зазоре1.95; поэтому окончательные
пороги2.8/3.4. После реальной перезагрузки на той же площадке eye/hidden=true,
cameraY=feetY+1.64; ступени и перила видны без головы. Console errors=[].
Вкладка1 сохранена markHandoff, не закрыта. Итоговая передача
`docs/city-rebuild/INDOOR_CAMERA_HANDOFF.md`.

Не трогать другие scoped vehicle/hero/traversal/artist hooks. Масштаб4.1, банк ID,
полиция, мост и серверные механики сохраняются; /walk по-прежнему проверочная сцена.

## Следующее поручение: автосход и Ctrl-спуск

Пользователь попросил автоматический выход с лестницы на крышу и быстрый спуск
скольжением по Ctrl. Подключены дополнительные 0.75 м на крышу с отпусканием рук,
скорость вертикального скольжения 5.5 м/с, Ctrl-разворот из подъёма и безопасное
замедление по E до земли. Все 6 реальных крыш проверены на дополнительный шаг.
LIVE малый банк: один E до полностью свободного положения на крыше y4.995;
один Ctrl до земли y0, поза скольжения осмотрена, active=false по завершении.
CPU controller/keyboard/геометрия и регрессии — PASS. Передача:
`docs/city-rebuild/ROOF_SLIDE_HANDOFF.md`.

Другая задача за время работы перевела /walk во world gateway. Для изолированной
проверки теперь нужен `?standalone=1&buildingqa=1`. Gateway не откатывать.
В общем Ctrl наземное действие другой владелец заменил на toggleHeroCover;
наш приоритет лестницы сохранён, актуальный keyboard regression проходит.
Backup начала этого поручения: `outputs/rooms-slide-qa/` (три ladder-модуля).
