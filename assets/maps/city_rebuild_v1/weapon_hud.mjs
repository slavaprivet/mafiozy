import {weaponIconSvg} from './weapon_icons.mjs';

export const SNIPER_SCOPE_SVG=`<svg class="mfz-scope-optic" viewBox="0 0 800 800" aria-hidden="true" focusable="false"><defs><radialGradient id="mfzScopeGlass"><stop offset="0" stop-color="#243b40" stop-opacity=".08"/><stop offset=".72" stop-color="#10252b" stop-opacity=".24"/><stop offset="1" stop-color="#030608" stop-opacity=".78"/></radialGradient></defs><circle cx="400" cy="400" r="326" fill="url(#mfzScopeGlass)" stroke="#b8954f" stroke-width="5"/><circle cx="400" cy="400" r="309" fill="none" stroke="#5f1723" stroke-width="9" opacity=".9"/><g fill="none" stroke="#ead8aa" stroke-width="2" opacity=".88"><path d="M88 400h242M470 400h242M400 88v242M400 470v242"/><path d="M245 390v20M280 386v28M315 390v20M485 390v20M520 386v28M555 390v20M390 245h20M386 280h28M390 315h20M390 485h20M386 520h28M390 555h20"/></g><circle cx="400" cy="400" r="7" fill="none" stroke="#ead8aa" stroke-width="2"/></svg>`;

export const WEAPON_HUD_CSS=`
.mfz-weapon-hud{position:absolute;right:18px;bottom:18px;z-index:43;width:min(430px,calc(100vw - 36px));font-family:Georgia,'Times New Roman',serif;color:#f3e5bd;filter:drop-shadow(0 12px 28px #000b)}
.mfz-weapon-launcher{width:100%;min-height:70px;display:grid;grid-template-columns:104px 1fr auto;align-items:center;gap:12px;padding:8px 12px;border:1px solid #b8954f;border-radius:4px 18px 4px 18px;background:linear-gradient(135deg,#18090d 0%,#45131d 48%,#140b0d 100%);color:inherit;text-align:left;box-shadow:inset 0 0 0 2px #080506,inset 0 1px #ead18b66;cursor:pointer}
.mfz-weapon-icon{display:block;width:100%;height:48px}.mfz-icon-body{fill:#0a0909;stroke:#d9c08a;stroke-width:1.4}.mfz-icon-accent{fill:#b8954f}
.mfz-weapon-title{display:block;font-weight:800;font-size:16px;letter-spacing:.04em}.mfz-weapon-ammo{display:block;margin-top:3px;font:800 13px system-ui;color:#d9c08a}.mfz-weapon-key{display:grid;place-items:center;width:34px;height:34px;border:1px solid #eed693;background:#b8954f;color:#1b0b0e;font:900 15px system-ui;box-shadow:inset 0 0 0 2px #6f5228}
.mfz-weapon-grid{position:absolute;right:0;bottom:78px;width:100%;max-height:min(72vh,610px);overflow:auto;display:grid;grid-template-columns:1fr 1fr;gap:7px;padding:10px;border:1px solid #b8954f;border-radius:18px 4px 18px 4px;background:linear-gradient(145deg,#10090bf7,#2d0e16fa);box-shadow:inset 0 0 0 2px #070405,0 18px 45px #000d}
.mfz-weapon-choice{min-height:76px;padding:7px 9px;border:1px solid #5d3f31;border-radius:3px 12px 3px 12px;background:#171315;color:#f0dfb6;text-align:left;cursor:pointer}.mfz-weapon-choice:hover,.mfz-weapon-choice:focus-visible{border-color:#e2c474;background:#31121a;outline:none}.mfz-weapon-choice[aria-selected=true]{border-color:#e2c474;background:linear-gradient(135deg,#5e1725,#2a0d13);box-shadow:inset 4px 0 #b8954f}.mfz-weapon-choice .mfz-weapon-icon{height:38px}.mfz-weapon-choice b,.mfz-weapon-choice small{display:block}.mfz-weapon-choice small{font:700 10px system-ui;color:#bda983;letter-spacing:.06em;text-transform:uppercase}
.mfz-weapon-hud[data-ammo-state=empty] .mfz-weapon-launcher{border-color:#ff6672;box-shadow:inset 0 0 0 2px #080506,0 0 20px #8b102b88}.mfz-weapon-hud[data-ammo-state=empty] .mfz-weapon-ammo{color:#ff8a92}.mfz-weapon-hud[data-ammo-state=reloading] .mfz-weapon-ammo{color:#f3ce68}.mfz-weapon-hud[data-disabled=true]{opacity:.5;pointer-events:none}
.mfz-sniper-scope{position:fixed;inset:0;z-index:80;display:grid;place-items:center;overflow:hidden;background:radial-gradient(circle at center,transparent 0 37vmin,#020304 42vmin 100%);pointer-events:none;opacity:0;visibility:hidden;transition:opacity .1s linear}.mfz-sniper-scope[data-visible=true]{opacity:1;visibility:visible}.mfz-scope-optic{width:min(92vmin,920px);height:min(92vmin,920px);filter:drop-shadow(0 0 28px #000)}
@media(max-width:760px){.mfz-weapon-hud{right:8px;bottom:106px;width:min(360px,calc(100vw - 16px))}.mfz-weapon-grid{grid-template-columns:1fr;max-height:58vh}.mfz-weapon-launcher{grid-template-columns:86px 1fr auto}}
`;

export function weaponHudViewState({weaponId='none',magazine=0,reserveAmmo=0,reloadRemaining=0}={}){
 const mag=Math.max(0,Math.floor(+magazine||0)),reserve=Math.max(0,Math.floor(+reserveAmmo||0)),reload=Math.max(0,+reloadRemaining||0);
 if(weaponId==='none')return Object.freeze({state:'unarmed',ammoText:'ОРУЖИЕ УБРАНО',magazine:0,reserveAmmo:0,reloadRemaining:0});
 if(reload>0)return Object.freeze({state:'reloading',ammoText:`ПЕРЕЗАРЯДКА · ${reload.toFixed(1)} С`,magazine:mag,reserveAmmo:reserve,reloadRemaining:reload});
 if(mag===0)return Object.freeze({state:'empty',ammoText:`ПУСТО · 0 / ${reserve}`,magazine:mag,reserveAmmo:reserve,reloadRemaining:0});
 return Object.freeze({state:'ready',ammoText:`${mag} / ${reserve}`,magazine:mag,reserveAmmo:reserve,reloadRemaining:0});
}

export function createWeaponHud({document:doc=globalThis.document,host,arsenal,onSelect=()=>{},onOpenChange=()=>{},bindKeyboard=false}={}){
 if(!doc?.createElement||!host||!Array.isArray(arsenal))throw Error('Weapon HUD requires document, host and arsenal');
 let style=doc.getElementById('mfz-weapon-hud-style'),ownsStyle=false;if(!style){style=doc.createElement('style');style.id='mfz-weapon-hud-style';style.textContent=WEAPON_HUD_CSS;doc.head.append(style);ownsStyle=true}
 host.replaceChildren();host.classList.add('mfz-weapon-hud');const grid=doc.createElement('div'),launcher=doc.createElement('button'),icon=doc.createElement('span'),copy=doc.createElement('span'),title=doc.createElement('b'),ammo=doc.createElement('small'),key=doc.createElement('kbd');
 grid.className='mfz-weapon-grid';grid.hidden=true;grid.setAttribute('role','listbox');grid.setAttribute('aria-label','Арсенал оружия');launcher.type='button';launcher.className='mfz-weapon-launcher';launcher.setAttribute('aria-expanded','false');icon.className='mfz-current-icon';title.className='mfz-weapon-title';ammo.className='mfz-weapon-ammo';key.className='mfz-weapon-key';key.textContent='Q';copy.append(title,ammo);launcher.append(icon,copy,key);host.append(grid,launcher);
 const scope=doc.createElement('div');scope.className='mfz-sniper-scope';scope.dataset.visible='false';scope.setAttribute('aria-hidden','true');scope.innerHTML=SNIPER_SCOPE_SVG;doc.body.append(scope);const byId=new Map(arsenal.map(item=>[item.id,item]));
 for(const item of arsenal){const button=doc.createElement('button');button.type='button';button.className='mfz-weapon-choice';button.dataset.weapon=item.id;button.setAttribute('role','option');button.setAttribute('aria-selected','false');button.innerHTML=`${weaponIconSvg(item.id)}<b>${item.label}</b><small>${item.id==='none'?'убрать оружие':item.twoHanded?'двуручный хват':'одноручный хват'}</small>`;button.addEventListener('click',()=>{if(!disabled)onSelect(item.id)});grid.append(button)}
 const choiceButtons=[...grid.querySelectorAll('.mfz-weapon-choice')];let currentId='',open=false,disabled=false,disposed=false,lastAmmoText='',lastAmmoState='';
 const setOpen=value=>{if(disposed)return false;const next=!!value&&!disabled;if(next===open)return open;open=next;grid.hidden=!open;launcher.setAttribute('aria-expanded',String(open));if(open)grid.querySelector(`[data-weapon="${currentId}"]`)?.focus();onOpenChange(open);return open};
 const setState=state=>{if(disposed)return;const item=byId.get(state.weaponId)||byId.get('none')||arsenal[0],view=weaponHudViewState({...state,weaponId:item.id}),nextDisabled=!!state.disabled;
  if(item.id!==currentId){currentId=item.id;icon.innerHTML=weaponIconSvg(item.id);title.textContent=item.label;host.dataset.weapon=item.id;for(const button of choiceButtons)button.setAttribute('aria-selected',String(button.dataset.weapon===item.id));if(item.id!=='sniper')setScopeVisible(false)}
  if(view.ammoText!==lastAmmoText){lastAmmoText=view.ammoText;ammo.textContent=view.ammoText}if(view.state!==lastAmmoState){lastAmmoState=view.state;host.dataset.ammoState=view.state}
  if(nextDisabled!==disabled){disabled=nextDisabled;host.dataset.disabled=String(disabled);launcher.disabled=disabled;for(const button of choiceButtons)button.disabled=disabled;if(disabled)setOpen(false)}return view};
 const setScopeVisible=value=>{const visible=!!value&&currentId==='sniper';scope.dataset.visible=String(visible);scope.setAttribute('aria-hidden',String(!visible));return visible};
 const handleKeydown=event=>{if(disabled||event.code!=='KeyQ'||event.repeat)return false;event.preventDefault();setOpen(!open);return true};
 launcher.addEventListener('click',()=>setOpen(!open));if(bindKeyboard)doc.addEventListener('keydown',handleKeydown,true);setState({weaponId:'none'});
 return {setState,setOpen,toggle:()=>setOpen(!open),isOpen:()=>open,setScopeVisible,handleKeydown,host,launcher,grid,scope,dispose(){if(disposed)return;disposed=true;if(open)onOpenChange(false);if(bindKeyboard)doc.removeEventListener('keydown',handleKeydown,true);scope.remove();host.replaceChildren();host.classList.remove('mfz-weapon-hud');if(ownsStyle)style.remove()}};
}
