import {createWeaponThumbnailRenderer} from './weapon_thumbnails.mjs';

export const SNIPER_SCOPE_SVG=`<svg class="mfz-scope-optic" viewBox="0 0 800 800" aria-hidden="true" focusable="false"><defs><radialGradient id="mfzScopeGlass"><stop offset="0" stop-color="#243b40" stop-opacity=".08"/><stop offset=".72" stop-color="#10252b" stop-opacity=".24"/><stop offset="1" stop-color="#030608" stop-opacity=".78"/></radialGradient></defs><circle cx="400" cy="400" r="326" fill="url(#mfzScopeGlass)" stroke="#b8954f" stroke-width="5"/><circle cx="400" cy="400" r="309" fill="none" stroke="#5f1723" stroke-width="9" opacity=".9"/><g fill="none" stroke="#ead8aa" stroke-width="2" opacity=".88"><path d="M88 400h242M470 400h242M400 88v242M400 470v242"/><path d="M245 390v20M280 386v28M315 390v20M485 390v20M520 386v28M555 390v20M390 245h20M386 280h28M390 315h20M390 485h20M386 520h28M390 555h20"/></g><circle cx="400" cy="400" r="7" fill="none" stroke="#ead8aa" stroke-width="2"/></svg>`;

export const WEAPON_HUD_CSS=`
.mfz-weapon-hud,#weapon-hud.mfz-weapon-hud{position:absolute;left:18px;right:auto;top:auto;bottom:18px;z-index:43;width:min(430px,calc(100vw - 36px));height:auto;min-height:0;padding:0;border:0;background:none;box-shadow:none;display:block;font:14px system-ui,sans-serif;color:#eeeae0;filter:drop-shadow(0 12px 28px #000b)}
.mfz-weapon-hud *{box-sizing:border-box}.mfz-weapon-hud [hidden]{display:none!important}
.mfz-weapon-launcher{width:100%;min-height:78px;display:grid;grid-template-columns:112px 1fr auto;align-items:center;gap:12px;padding:7px 13px;border:1px solid #78746a;border-radius:6px;background:linear-gradient(135deg,#34383b,#15191c 75%);color:inherit;text-align:left;box-shadow:inset 0 1px #ffffff20,inset 0 -3px #080a0b;cursor:pointer}
.mfz-current-icon{height:62px;display:grid;place-items:center;background:radial-gradient(ellipse,#74808b24,transparent 72%)}
.mfz-weapon-photo{display:block;width:100%;height:100%;object-fit:contain;filter:drop-shadow(0 5px 4px #0009)}
.mfz-weapon-title{display:block;font-weight:700;font-size:14px;letter-spacing:.015em}.mfz-weapon-ammo{display:block;margin-top:5px;font:700 12px ui-monospace,monospace;color:#b8c5cd}.mfz-weapon-key{display:grid;place-items:center;width:30px;height:30px;border:1px solid #858582;border-radius:4px;background:linear-gradient(#3b4145,#252a2e);color:#eeeae0;font:700 13px system-ui;box-shadow:0 2px #080a0b}
.mfz-weapon-grid{position:absolute;left:0;right:auto;bottom:90px;width:min(690px,calc(100vw - 36px));max-height:min(74vh,700px);overflow:auto;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;padding:15px;border:1px solid #77766f;border-radius:8px;background:linear-gradient(125deg,#292e32fc,#111619fc);box-shadow:inset 0 1px #ffffff25,0 18px 45px #000d;scrollbar-width:thin;scrollbar-color:#767464 #191d20}
.mfz-arsenal-heading,.mfz-arsenal-footer{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:14px}.mfz-arsenal-heading{padding:1px 2px 12px;border-bottom:1px solid #70716b55}.mfz-arsenal-heading b{display:block;font-size:18px;letter-spacing:.16em}.mfz-arsenal-heading small{display:block;margin-top:5px;color:#a9b1b5;font-size:11px;letter-spacing:.06em}.mfz-arsenal-close{border:1px solid #6d7376;background:#23292d;border-radius:4px;width:30px;height:30px;color:#d7dbda;font-size:20px;cursor:pointer}.mfz-arsenal-footer{padding:10px 2px 0;border-top:1px solid #70716b55;flex-wrap:wrap;color:#aeb7bc;font-size:11px}.mfz-arsenal-footer kbd{color:#e7d6ad;border:1px solid #73716c;border-radius:3px;padding:1px 5px;margin-right:4px;font:700 11px system-ui}
.mfz-weapon-choice{position:relative;min-height:139px;padding:8px 10px 10px;border:1px solid #4f595f;border-radius:5px;background:linear-gradient(145deg,#2b343b,#1b2227);color:#e6e9e7;text-align:left;cursor:pointer;box-shadow:inset 0 1px #ffffff0c,0 2px 5px #0005}.mfz-weapon-choice::before{content:'';position:absolute;inset:5px;border:1px solid #0002;border-radius:3px;pointer-events:none}.mfz-weapon-choice:hover,.mfz-weapon-choice:focus-visible{border-color:#d1b77d;background:linear-gradient(145deg,#3b4449,#252e33);outline:2px solid #cab27a55;outline-offset:1px}.mfz-weapon-choice[aria-selected=true]{border-color:#c9ac6b;background:linear-gradient(145deg,#4a4940,#2c302e);box-shadow:inset 0 2px #c9ac6b,0 2px 5px #0005}.mfz-weapon-display{display:grid;place-items:center;height:79px;margin-bottom:8px;border-bottom:1px solid #b2bfca18;background:radial-gradient(ellipse,#b4c4d325,transparent 72%)}.mfz-weapon-placeholder{color:#b9c4c9;font-size:11px;letter-spacing:.08em}.mfz-weapon-choice b,.mfz-weapon-choice small{display:block;position:relative}.mfz-weapon-choice b{font-size:12px;font-weight:650}.mfz-weapon-choice small{margin-top:5px;font:500 10px system-ui;color:#9eafb9;letter-spacing:.03em}.mfz-weapon-choice[aria-selected=true] small{color:#e0c58b}
.mfz-weapon-hud[data-ammo-state=empty] .mfz-weapon-launcher{border-color:#ce6f67;box-shadow:inset 0 -3px #713b35}.mfz-weapon-hud[data-ammo-state=empty] .mfz-weapon-ammo{color:#f29b91}.mfz-weapon-hud[data-ammo-state=reloading] .mfz-weapon-ammo{color:#e3c780}.mfz-weapon-hud[data-disabled=true]{opacity:.5;pointer-events:none}
.mfz-sniper-scope{position:fixed;inset:0;z-index:80;display:grid;place-items:center;overflow:hidden;background:radial-gradient(circle at center,transparent 0 37vmin,#020304 42vmin 100%);pointer-events:none;opacity:0;visibility:hidden;transition:opacity .1s linear}.mfz-sniper-scope[data-visible=true]{opacity:1;visibility:visible}.mfz-scope-optic{width:min(92vmin,920px);height:min(92vmin,920px);filter:drop-shadow(0 0 28px #000)}
@media(max-width:760px){.mfz-weapon-hud,#weapon-hud.mfz-weapon-hud{left:8px;right:auto;bottom:106px;width:min(360px,calc(100vw - 16px))}.mfz-weapon-grid{width:min(540px,calc(100vw - 16px));grid-template-columns:repeat(2,minmax(0,1fr));max-height:58vh;padding:10px;gap:8px}.mfz-weapon-launcher{grid-template-columns:86px 1fr auto}.mfz-weapon-choice{min-height:125px}.mfz-weapon-display{height:66px}}
`;

// The gateway mounts /walk inside ShadowRoot; document styles cannot cross it.
// Acquire once per owning root and document (fullscreen scope lives in body).
const weaponHudStyleRoots=new WeakMap();
export function acquireWeaponHudStyles(doc,host){
 const owner=host?.getRootNode?.(),roots=new Set([doc]);if(owner&&owner!==doc&&owner.host)roots.add(owner);
 const acquired=[];
 for(const root of roots){let record=weaponHudStyleRoots.get(root);if(!record){let style=root.getElementById?.('mfz-weapon-hud-style')||root.querySelector?.('#mfz-weapon-hud-style'),owned=false;if(!style){style=doc.createElement('style');style.id='mfz-weapon-hud-style';style.textContent=WEAPON_HUD_CSS;(root.head||root).append(style);owned=true;}record={style,owned,references:0};weaponHudStyleRoots.set(root,record);}record.references++;acquired.push([root,record]);}
 let released=false;return ()=>{if(released)return;released=true;for(const[root,record]of acquired){record.references--;if(!record.references){if(record.owned)record.style.remove();weaponHudStyleRoots.delete(root);}}};
}

export function weaponHudViewState({weaponId='none',magazine=0,reserveAmmo=0,reloadRemaining=0}={}){
 const mag=Math.max(0,Math.floor(+magazine||0)),reserve=Math.max(0,Math.floor(+reserveAmmo||0)),reload=Math.max(0,+reloadRemaining||0);
 if(weaponId==='none')return Object.freeze({state:'unarmed',ammoText:'ОРУЖИЕ УБРАНО',magazine:0,reserveAmmo:0,reloadRemaining:0});
 if(reload>0)return Object.freeze({state:'reloading',ammoText:`ПЕРЕЗАРЯДКА · ${reload.toFixed(1)} С`,magazine:mag,reserveAmmo:reserve,reloadRemaining:reload});
 if(mag===0)return Object.freeze({state:'empty',ammoText:`ПУСТО · 0 / ${reserve}`,magazine:mag,reserveAmmo:reserve,reloadRemaining:0});
 return Object.freeze({state:'ready',ammoText:`${mag} / ${reserve}`,magazine:mag,reserveAmmo:reserve,reloadRemaining:0});
}

export function weaponHudOwnedIds(arsenal,ownedWeaponIds){
 const requested=Array.isArray(ownedWeaponIds)?new Set(ownedWeaponIds):null;
 return arsenal.filter(item=>item.id==='none'||!requested||requested.has(item.id)).map(item=>item.id);
}

const familyLabels={revolver:'РЕВОЛЬВЕР',pistol:'ПИСТОЛЕТ','heavy-pistol':'ТЯЖЁЛЫЙ ПИСТОЛЕТ',shotgun:'ДРОБОВИК',smg:'ПИСТОЛЕТ-ПУЛЕМЁТ',rifle:'АВТОМАТ',sniper:'ВИНТОВКА',launcher:'ГРАНАТОМЁТ'};
export function createWeaponHud({document:doc=globalThis.document,host,arsenal,THREE,onSelect=()=>{},onOpenChange=()=>{},bindKeyboard=false,thumbnailRenderer}={}){
 if(!doc?.createElement||!host||!Array.isArray(arsenal))throw Error('Weapon HUD requires document, host and arsenal');
 const releaseStyles=acquireWeaponHudStyles(doc,host);
 host.replaceChildren();host.classList.add('mfz-weapon-hud');const grid=doc.createElement('div'),launcher=doc.createElement('button'),icon=doc.createElement('span'),copy=doc.createElement('span'),title=doc.createElement('b'),ammo=doc.createElement('small'),key=doc.createElement('kbd');
 grid.className='mfz-weapon-grid';grid.hidden=true;grid.setAttribute('role','group');grid.setAttribute('aria-label','Арсенал — имеющееся оружие');launcher.type='button';launcher.className='mfz-weapon-launcher';launcher.setAttribute('aria-expanded','false');launcher.setAttribute('aria-label','Открыть арсенал — Q');icon.className='mfz-current-icon';title.className='mfz-weapon-title';ammo.className='mfz-weapon-ammo';key.className='mfz-weapon-key';key.textContent='Q';copy.append(title,ammo);launcher.append(icon,copy,key);host.append(grid,launcher);
 const scope=doc.createElement('div');scope.className='mfz-sniper-scope';scope.dataset.visible='false';scope.setAttribute('aria-hidden','true');scope.innerHTML=SNIPER_SCOPE_SVG;doc.body.append(scope);const byId=new Map(arsenal.map(item=>[item.id,item]));
 const heading=doc.createElement('div'),headingCopy=doc.createElement('span'),headingTitle=doc.createElement('b'),count=doc.createElement('small'),close=doc.createElement('button');heading.className='mfz-arsenal-heading';headingTitle.textContent='АРСЕНАЛ';close.type='button';close.className='mfz-arsenal-close';close.textContent='×';close.setAttribute('aria-label','Закрыть арсенал');headingCopy.append(headingTitle,count);heading.append(headingCopy,close);grid.append(heading);
 const thumbnails=thumbnailRenderer||createWeaponThumbnailRenderer({THREE,document:doc}),choices=new Map(),images=new Map(),attempted=new Set(),pending=[];let frame=null,choicesDirty=true;
 const schedule=doc.defaultView?.requestAnimationFrame?.bind(doc.defaultView)||globalThis.requestAnimationFrame||((callback)=>setTimeout(callback,0)),cancel=doc.defaultView?.cancelAnimationFrame?.bind(doc.defaultView)||globalThis.cancelAnimationFrame||clearTimeout;
 let currentId='',open=false,disabled=false,disposed=false,lastAmmoText='',lastAmmoState='',scopeVisible=false,owned=new Set(weaponHudOwnedIds(arsenal)),ammoByWeaponId={};
 const imageFor=(container,item)=>{container.replaceChildren();if(images.has(item.id)){const img=doc.createElement('img');img.className='mfz-weapon-photo';img.src=images.get(item.id);img.alt='';img.setAttribute('aria-hidden','true');container.append(img)}else{const placeholder=doc.createElement('span');placeholder.className='mfz-weapon-placeholder';placeholder.textContent=item.id==='none'?'БЕЗ ОРУЖИЯ':'3D · '+(familyLabels[item.family]||'ОРУЖИЕ');container.append(placeholder)}};
 const requestThumbnail=id=>{if(attempted.has(id)||pending.includes(id)||disposed)return;pending.push(id);if(frame!==null)return;const step=()=>{frame=null;if(disposed)return;const next=pending.shift();attempted.add(next);const url=thumbnails.render(next);if(url){images.set(next,url);imageFor(choices.get(next).display,byId.get(next));if(currentId===next)imageFor(icon,byId.get(next))}if(pending.length)frame=schedule(step)};frame=schedule(step)};
 const refreshChoices=()=>{let total=0;for(const [id,choice] of choices){const available=owned.has(id),selected=id===currentId,nextHidden=!available,nextDisabled=disabled||!available,nextSelected=String(selected);if(choice.button.hidden!==nextHidden)choice.button.hidden=nextHidden;if(choice.button.disabled!==nextDisabled)choice.button.disabled=nextDisabled;if(choice.button.getAttribute('aria-selected')!==nextSelected)choice.button.setAttribute('aria-selected',nextSelected);if(choice.button.getAttribute('aria-pressed')!==nextSelected)choice.button.setAttribute('aria-pressed',nextSelected);if(available&&id!=='none')total++;const rounds=ammoByWeaponId[id],ammoSuffix=rounds?` · ${Math.max(0,Math.floor(+rounds.magazine||0))} / ${Math.max(0,Math.floor(+rounds.reserveAmmo||0))}`:'';const text=id==='none'?(selected?'РУКИ СВОБОДНЫ':'УБРАТЬ ОРУЖИЕ'):(selected?'В РУКАХ':familyLabels[byId.get(id).family]||'ОРУЖИЕ')+ammoSuffix;if(choice.detail.textContent!==text)choice.detail.textContent=text;if(open&&available)requestThumbnail(id)}const countText=`ПРИ СЕБЕ: ${total} · ВЫБЕРИТЕ ОРУЖИЕ`;if(count.textContent!==countText)count.textContent=countText};
 for(const item of arsenal){const button=doc.createElement('button'),display=doc.createElement('span'),label=doc.createElement('b'),detail=doc.createElement('small');button.type='button';button.className='mfz-weapon-choice';button.dataset.weapon=item.id;display.className='mfz-weapon-display';label.textContent=item.label;button.setAttribute('aria-label',item.label);imageFor(display,item);button.append(display,label,detail);button.addEventListener('click',()=>{if(!disposed&&!disabled&&owned.has(item.id))onSelect(item.id)});choices.set(item.id,{button,display,detail});grid.append(button)}
 const footer=doc.createElement('div');footer.className='mfz-arsenal-footer';footer.innerHTML='<span><kbd>Q</kbd> арсенал</span><span><kbd>G</kbd> выбросить</span><span><kbd>E</kbd> подобрать рядом</span>';grid.append(footer);
 const syncOwned=value=>{let changed=false;if(Array.isArray(value)){for(const item of arsenal){const expected=item.id==='none'||value.includes(item.id);if(owned.has(item.id)!==expected){changed=true;break}}}else changed=owned.size!==arsenal.length;if(changed)owned=new Set(weaponHudOwnedIds(arsenal,value));return changed};
 const setOpen=value=>{if(disposed)return false;const next=!!value&&!disabled;if(next===open)return open;open=next;grid.hidden=!open;launcher.setAttribute('aria-expanded',String(open));launcher.setAttribute('aria-label',open?'Закрыть арсенал — Q':'Открыть арсенал — Q');if(open){refreshChoices();choicesDirty=false;choices.get(currentId)?.button.focus()}onOpenChange(open);return open};
 const setState=(state={})=>{if(disposed)return;if(Object.prototype.hasOwnProperty.call(state,'ownedWeaponIds')&&syncOwned(state.ownedWeaponIds))choicesDirty=true;if(state.ammoByWeaponId&&state.ammoByWeaponId!==ammoByWeaponId){ammoByWeaponId=state.ammoByWeaponId;choicesDirty=true}
  const candidate=byId.get(state.weaponId)||byId.get('none')||arsenal[0],item=owned.has(candidate.id)?candidate:(byId.get('none')||arsenal.find(entry=>owned.has(entry.id))),view=weaponHudViewState({...state,weaponId:item.id}),nextDisabled=!!state.disabled;
  if(item.id!==currentId){currentId=item.id;choicesDirty=true;imageFor(icon,item);requestThumbnail(item.id);title.textContent=item.label;host.dataset.weapon=item.id;if(item.id!=='sniper')setScopeVisible(false)}
  if(view.ammoText!==lastAmmoText){lastAmmoText=view.ammoText;ammo.textContent=view.ammoText}if(view.state!==lastAmmoState){lastAmmoState=view.state;host.dataset.ammoState=view.state}
  if(nextDisabled!==disabled){disabled=nextDisabled;choicesDirty=true;host.dataset.disabled=String(disabled);launcher.disabled=disabled;if(disabled)setOpen(false)}if(choicesDirty){refreshChoices();choicesDirty=false}return view};
 const setScopeVisible=value=>{const visible=!!value&&currentId==='sniper';if(visible===scopeVisible)return visible;scopeVisible=visible;scope.dataset.visible=String(visible);scope.setAttribute('aria-hidden',String(!visible));return visible};
 const handleKeydown=event=>{if(disposed||disabled||event.code!=='KeyQ'||event.repeat||event.target?.isContentEditable||['INPUT','TEXTAREA','SELECT'].includes(event.target?.tagName))return false;event.preventDefault();setOpen(!open);return true};
 launcher.addEventListener('click',()=>setOpen(!open));close.addEventListener('click',()=>setOpen(false));if(bindKeyboard)doc.addEventListener('keydown',handleKeydown,true);setState({weaponId:'none'});
 return {setState,setOpen,toggle:()=>setOpen(!open),isOpen:()=>open,setScopeVisible,handleKeydown,host,launcher,grid,scope,dispose(){if(disposed)return;disposed=true;if(frame!==null)cancel(frame);pending.length=0;thumbnails.dispose();if(open)onOpenChange(false);if(bindKeyboard)doc.removeEventListener('keydown',handleKeydown,true);scope.remove();host.replaceChildren();host.classList.remove('mfz-weapon-hud');releaseStyles()}};
}
