// Captured target, fresh source eligibility at selection; never owns gameplay.
export function createMercenaryActionMenu({document:doc,parent=doc.body,getTarget,getActions,onChoose,onOpenChange=()=>{}}={}){
 const root=doc.createElement('section'),title=doc.createElement('strong'),buttons=doc.createElement('div'),status=doc.createElement('p'),closeButton=doc.createElement('button');
 root.dataset.walkHud='mercenary-action-menu';root.setAttribute('role','dialog');root.setAttribute('aria-label','Выберите действие');root.setAttribute('aria-modal','true');root.style.cssText='position:fixed;left:50%;top:55%;transform:translateX(-50%);z-index:10010;min-width:210px;max-width:320px;padding:12px;border:1px solid #b29c71;border-radius:12px;background:#17211ff2;color:#f1e8d0;font:14px/1.4 system-ui;box-shadow:0 6px 22px #0006;pointer-events:auto;display:none';
 title.textContent='Выберите действие';buttons.style.cssText='display:grid;gap:6px;margin-top:9px';status.setAttribute('role','status');status.style.cssText='margin:7px 0;color:#e7b59a;font-size:12px';closeButton.type='button';closeButton.textContent='Отмена · Esc';closeButton.style.cssText='margin-top:7px;border:0;background:transparent;color:#b8c3b7;cursor:pointer';root.append(title,buttons,status,closeButton);root.hidden=true;parent.append(root);
 const cards=new Map();let targetId=null,opened=false,pending=false,disposed=false,elapsed=.2;
 const eligible=t=>t&&t.valid!==false&&!t.destroyed&&!t.wrecked&&!t.opened&&!t.cut&&!t.completed;
 function close(){if(!opened)return;opened=false;targetId=null;root.hidden=true;root.style.display='none';onOpenChange(false);}
 function refresh(){if(disposed||!opened)return;const target=getTarget(targetId);if(!eligible(target)){close();return;}const actions=getActions(target)||[],seen=new Set();for(const a of actions){const id=a.id||a.kind;if(!id||seen.has(id))continue;seen.add(id);let b=cards.get(id);if(!b){b=doc.createElement('button');b.type='button';b.dataset.actionId=id;b.style.cssText='padding:9px 13px;border:1px solid #718e77;border-radius:7px;background:#284032;color:#ecf1dc;text-align:left;font:600 14px system-ui;cursor:pointer';b.addEventListener('click',()=>void choose(id));cards.set(id,b);buttons.append(b);}b.textContent=(a.willQueue?'В очередь · ':'')+(a.label||id);b.disabled=pending;b.hidden=false;}
  for(const[id,b]of cards)if(!seen.has(id)){b.hidden=true;b.disabled=true;}if(!seen.size&&!pending){status.textContent='Действия больше недоступны.';}
 }
 async function choose(id){if(disposed||!opened||pending)return;const captured=targetId,target=getTarget(captured),action=eligible(target)&&(getActions(target)||[]).find(a=>(a.id||a.kind)===id);if(!action){status.textContent='Цель или специалист больше недоступны.';refresh();return;}pending=true;status.textContent='';refresh();try{const result=await onChoose(id,target);if(disposed||!opened||captured!==targetId)return;if(result?.ok===true)close();else status.textContent=result?.message||result?.reason||'Приказ не принят.';}catch(error){if(!disposed&&opened)status.textContent=error?.message||'Приказ не принят.';}finally{pending=false;refresh();}}
 function open(target){if(disposed||pending||!eligible(target))return false;if(opened)close();targetId=String(target.id);opened=true;status.textContent='';root.hidden=false;root.style.display='block';onOpenChange(true);refresh();[...cards.values()].find(b=>!b.hidden&&!b.disabled)?.focus?.();return opened;}
 const key=e=>{if(!opened||e.repeat||e.defaultPrevented)return;if(e.code==='KeyX'||e.code==='Escape'||e.key==='Escape'){e.preventDefault();e.stopImmediatePropagation?.();close();}};doc.addEventListener('keydown',key,true);closeButton.addEventListener('click',close);for(const type of ['pointerdown','pointerup','mousedown','mouseup','click','dblclick','wheel'])root.addEventListener(type,e=>e.stopPropagation());
 return{element:root,open,close,update(dt=0){elapsed+=Math.max(0,dt);if(elapsed>=.2){elapsed=0;refresh();}},get isOpen(){return opened;},get targetId(){return targetId;},dispose(){if(disposed)return;close();disposed=true;doc.removeEventListener('keydown',key,true);root.remove();cards.clear();}};
}
