// One world-anchored subtitle. NPC visibility/occlusion is shared with badges.
export function createMercenarySpeechBubble({THREE,document:doc,parent=doc.body,camera,getBadgeAnchor=()=>null,getHero=()=>null,getFocus=()=>null}={}){
 const node=doc.createElement('div');node.dataset.mercenaryChatter='true';node.dataset.mercenarySpeechBubble='true';node.setAttribute('role','status');node.style.cssText='position:fixed;transform:translate(-50%,-100%);max-width:min(280px,75vw);padding:8px 12px;border:1px solid #bbd4bcc9;border-radius:12px 12px 12px 3px;background:#15221deb;color:#f0f2db;box-shadow:0 3px 10px #0005;font:600 14px/1.35 system-ui;text-align:center;pointer-events:none;z-index:44;display:none';parent.append(node);
 const world=new THREE.Vector3(),clip=new THREE.Vector3(),eye=new THREE.Vector3(),headCache=new WeakMap();let line=null,disposed=false;
 const hide=()=>{node.hidden=true;node.style.display='none';};hide();
 function heroAnchor(w,h){const object=getHero();if(object){let head=headCache.get(object);if(head===undefined){head=null;object.traverse(n=>{if(!head&&(n.isBone&&/head(?:$|[_ .])/i.test(n.name||'')||/^head$/i.test(n.name||'')))head=n;});headCache.set(object,head);}let attached=false;for(let p=object;p;p=p.parent){if(!p.visible)return null;if(p.isScene)attached=true;}if(!attached)return null;if(head){head.getWorldPosition(world);world.y+=.55;}else{object.getWorldPosition(world);world.y+=2.1;}}else{const focus=getFocus();if(!focus)return null;world.set(focus.x,focus.y+2.1,focus.z);}camera.updateWorldMatrix(true,false);eye.copy(world).applyMatrix4(camera.matrixWorldInverse);clip.copy(world).project(camera);if(eye.z>=0||clip.z< -1||clip.z>1||Math.abs(clip.x)>1||Math.abs(clip.y)>1)return null;return{x:(clip.x*.5+.5)*w,y:(-clip.y*.5+.5)*h-10};}
 function update(){if(disposed||!line||doc.hidden){hide();return;}const w=parent.clientWidth||doc.documentElement?.clientWidth||doc.defaultView?.innerWidth||0,h=parent.clientHeight||doc.documentElement?.clientHeight||doc.defaultView?.innerHeight||0;if(!w||!h){hide();return;}const anchor=line.speakerId==='player'?heroAnchor(w,h):getBadgeAnchor(line.speakerId);if(!anchor||anchor.x<0||anchor.x>w||anchor.y<12||anchor.y>h){hide();return;}node.style.left=anchor.x.toFixed(1)+'px';node.style.top=anchor.y.toFixed(1)+'px';node.hidden=false;node.style.display='block';}
 return{element:node,setLine(value){line=value||null;node.textContent=line?String(line.text||''):'';node.dataset.speakerId=line?String(line.speakerId):'';if(line)node.setAttribute('aria-label',(line.name?line.name+': ':'')+line.text);update();},update,dispose(){if(disposed)return;disposed=true;line=null;node.remove();}};
}
